Fix: address Gemini review suggestions (performance and correctness fixes)

This commit implements the fixes requested by the Gemini code reviewer:
- Reuse enemy texture and sprite material to avoid per-enemy texture loads
- Initialize reward flag on enemies to avoid duplicate rewards
- Precompute path segment distances to avoid per-frame math
- Move enemy removal side-effects into WaveManager with callbacks for kill/reached
- Reuse projectile geometry/material to avoid allocations
- Reuse a single raycast plane and raycaster instead of recreating per click
- Use squared-distance checks for tower targeting for performance
- Use selected tower type from UI and replace alert() with non-blocking notification

LLM-Model: gpt-5-mini (GPT-5 mini)
Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
